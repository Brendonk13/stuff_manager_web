import { defaultProject, type Project, EditProjectSchema } from "@/types/Action"


export default function EditProjectForm(project: Project){


  // the form maybe should be in the parent
  // then I can switch between showing form and edit fields
  return (
        // <Box sx={{ width: '100%', padding: 2, }} component="form" onSubmit={handleSubmit(onSubmit)}>
        //   <FormProvider {...methods}>
  )
}
